import { DatePipe } from '@angular/common';import { ChangeDetectionStrategy, Component, inject, signal } from '@angular/core';import { UserSummary } from '../../../core/auth/models/auth.model';import { AuthService } from '../../../core/auth/services/auth.service';import { AdminApiService } from '../data-access/admin-api.service';
@Component({selector:'app-admin-users',changeDetection:ChangeDetectionStrategy.OnPush,imports:[DatePipe],template:`<div class="heading"><div><p>Access management</p><h1>Users</h1><span>Inspect every registered account and its permissions.</span></div><button (click)="load()">Refresh</button></div><section><div class="toolbar"><strong>{{users().length}} accounts</strong><input aria-label="Search users" placeholder="Search name or email" #query (input)="filter(query.value)"></div>@if(error()){<p class="error">{{error()}}</p>}@else if(loading()){<p class="state">Loading users…</p>}@else{<div class="table-wrap"><table><thead><tr><th>User</th><th>Contact</th><th>Role</th><th>Joined</th>@if(isSuperAdmin()){<th>Actions</th>}</tr></thead><tbody>@for(user of filtered();track user.id){<tr><td><div class="person"><span>{{user.prenom[0]}}{{user.nom[0]}}</span><div><strong>{{user.prenom}} {{user.nom}}</strong><small>#{{user.id}}</small></div></div></td><td><strong>{{user.email}}</strong><small>{{user.mobile}}</small></td><td><div class="roles">@for(role of user.roles;track role){<em>{{role}}</em>}</div></td><td>{{user.createdAt|date:'mediumDate'}}</td>@if(isSuperAdmin()){<td><button class="delete" [disabled]="deletingId()===user.id" (click)="deleteUser(user)">{{deletingId()===user.id?'Deleting…':'Delete'}}</button></td>}</tr>}@empty{<tr><td [attr.colspan]="isSuperAdmin()?5:4" class="empty">No matching users.</td></tr>}</tbody></table></div>}</section>`,styles:`.heading{display:flex;justify-content:space-between;align-items:end;margin-bottom:26px}.heading p{margin:0;color:var(--color-teal-600);text-transform:uppercase;letter-spacing:.1em;font-size:11px;font-weight:800}.heading h1{font-size:32px;margin:5px 0}.heading span{color:var(--color-text-muted)}button{border:1px solid var(--color-border-strong);background:#fff;padding:10px 16px;border-radius:7px;font-weight:700;cursor:pointer}button:disabled{opacity:.6;cursor:not-allowed}.delete{color:var(--color-danger);border-color:var(--color-danger);padding:7px 11px}.delete:hover:not(:disabled){background:var(--color-danger);color:#fff}section{background:#fff;border:1px solid var(--color-border);border-radius:10px;overflow:hidden}.toolbar{display:flex;align-items:center;justify-content:space-between;padding:18px 20px;border-bottom:1px solid var(--color-border)}input{width:min(320px,50%);padding:10px 12px;border:1px solid var(--color-border-strong);border-radius:7px;font:inherit}.table-wrap{overflow:auto}table{border-collapse:collapse;width:100%;min-width:720px}th,td{text-align:left;padding:15px 20px;border-bottom:1px solid var(--color-border);font-size:13px}th{color:var(--color-text-muted);font-size:11px;text-transform:uppercase;letter-spacing:.06em;background:var(--color-surface-subtle)}td>small,.person small{display:block;color:var(--color-text-muted);margin-top:3px}.person{display:flex;align-items:center;gap:11px}.person>span{display:grid;place-items:center;width:36px;height:36px;border-radius:50%;background:var(--color-teal-100);color:var(--color-teal-600);font-weight:800}.roles{display:flex;gap:5px}.roles em{font-style:normal;font-size:10px;font-weight:800;padding:4px 7px;border-radius:99px;background:var(--color-amber-100);color:var(--color-amber-600)}.state,.error,.empty{padding:30px;text-align:center}.error{color:var(--color-danger)}@media(max-width:650px){.heading{align-items:start}.toolbar{align-items:start;gap:12px;flex-direction:column}input{width:100%;box-sizing:border-box}}`})
export class AdminUsersComponent{private readonly api=inject(AdminApiService);private readonly auth=inject(AuthService);protected readonly users=signal<UserSummary[]>([]);protected readonly filtered=signal<UserSummary[]>([]);protected readonly loading=signal(true);protected readonly error=signal('');protected readonly deletingId=signal<number|null>(null);protected readonly isSuperAdmin=()=>this.auth.hasRole(['SUPER_ADMIN']);constructor(){this.load()}protected load(){this.loading.set(true);this.error.set('');this.api.users().subscribe({next:v=>{this.users.set(v);this.filtered.set(v)},error:()=>{this.error.set('Unable to load users.');this.loading.set(false)},complete:()=>this.loading.set(false)})}protected filter(value:string){const q=value.toLowerCase().trim();this.filtered.set(this.users().filter(u=>`${u.prenom} ${u.nom} ${u.email}`.toLowerCase().includes(q)))}protected deleteUser(user:UserSummary){if(!confirm(`Delete ${user.prenom} ${user.nom}? This action cannot be undone.`))return;this.deletingId.set(user.id);this.error.set('');this.api.deleteUser(user.id).subscribe({next:()=>{this.users.update(v=>v.filter(u=>u.id!==user.id));this.filtered.update(v=>v.filter(u=>u.id!==user.id))},error:()=>{this.error.set('Unable to delete this user.');this.deletingId.set(null)},complete:()=>this.deletingId.set(null)})}}
